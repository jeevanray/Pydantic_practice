To update `update_audit_record_strict` to work correctly **while keeping the variables as strings**, you should update the SQL query inside the function to handle string dates properly by using `TO_DATE` in the merge statement. Also, modify the SQL so that it updates fields like `task_startts` and `business_loaddt` **only if they are null**, preventing unwanted overwrites.

I reviewed your attached file and here is a safe patch for your merge SQL inside `update_audit_record_strict` function to use string date variables and conditionally update those timestamp fields:

***

### Code patch for `update_audit_record_strict`:

Inside your merge SQL, update the historic and delta sections' `WHEN MATCHED THEN UPDATE SET` part like so:

```sql
WHEN MATCHED THEN UPDATE SET
    delta_column_value = :delta_column_value,
    task_startts = NVL(tgt.task_startts, TO_DATE(:task_startts, 'YYYY-MM-DD HH24:MI:SS')),
    task_endts = TO_DATE(:task_endts, 'YYYY-MM-DD HH24:MI:SS'),
    task_exec_secs = :task_exec_secs,
    total_records = :total_records,
    extraction_time = :extraction_time,
    total_apicalls = :total_apicalls,
    success_apicalls = :success_apicalls,
    failed_apicalls = :failed_apicalls,
    api_failedpath = :api_failedpath,
    apicall_time = :apicall_time,
    cdp_db_count_validation = :cdp_db_count_validation,
    aerospike_init_record_cnt = :aerospike_init_record_cnt,
    aerospike_init_read_waittime = :aerospike_init_read_waittime,
    aerospike_record_cnt = :aerospike_record_cnt,
    aerospike_waittime = :aerospike_waittime,
    aerospike_error = :aerospike_error,
    mongodb_init_record_cnt = :mongodb_init_record_cnt,
    mongodb_record_cnt = :mongodb_record_cnt,
    mongodb_init_read_waittime = :mongodb_init_read_waittime,
    mongodb_waittime = :mongodb_waittime,
    mongodb_error = :mongodb_error,
    suspected_updates_or_blacklisted_records = :suspected_updates_or_blacklisted_records,
    difference_aero_mongo = :difference_aero_mongo,
    business_loaddt = NVL(tgt.business_loaddt, TO_DATE(:business_loaddt, 'YYYY-MM-DD')),
    status = :status,
    log_path = :log_path,
    minio_filepath = :minio_filepath,
    restart_point = :restart_point,
    updated_at_ts = TO_DATE(:updated_at_ts, 'YYYY-MM-DD HH24:MI:SS')
```

Notes:

- Use `TO_DATE(:fieldname, 'YYYY-MM-DD')` or `TO_DATE(:fieldname, 'YYYY-MM-DD HH24:MI:SS')` depending on the field.
- Use `NVL(tgt.fieldname, TO_DATE(:fieldname, ...))` to update only when target field is NULL.
- Make sure you use **aliases** consistently in the `MERGE` statement (usually alias the target table as `tgt` or `target`).

***

### Why this works:

- You pass date/time values as **strings** in your Python code.
- Oracle converts with `TO_DATE` in SQL safely.
- `NVL` prevents overwriting existing values if they are not null—for fields like `task_startts` and `business_loaddt`.
- `task_endts` and `updated_at_ts` are always updated with latest value.

***

### Summary

Modify the `update_audit_record_strict` merge SQL `UPDATE` set to use `TO_DATE` around string bindings for date columns, and `NVL` for conditional updates in order to keep your current variable usage (strings) intact while fixing your errors and overwrite issues.

If you want, I can extract your exact `update_audit_record_strict` function from your attached file and produce a fully patched version for you to replace. Would you like me to do that?
